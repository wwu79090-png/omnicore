# OmniCore RPG Template

Use this template for tilemap exploration, party stats, inventory, skills, dialogue, and save slots.

Recommended modules:

- `Scene`, `Tilemap`, `NavigationAgent2D`
- `Database`, `DataTable`, `EventSheet`
- `Storage.saveSlot()`
- `AudioManager.configureStandardBuses()`
