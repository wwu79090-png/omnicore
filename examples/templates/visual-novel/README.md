# OmniCore Visual Novel Template

Use this template for HTML overlays, text input, branching dialogue, background transitions, and legacy localStorage migration.

Recommended modules:

- `HtmlOverlay`
- `Input.pointer.enableEventPropagation()`
- `Storage.importLegacy()`
- `Store.snapshot()`
