# Performance Hot Paths Demo

This demo renders deterministic evidence for OmniCore's performance hot paths:

- render queue batching
- runtime object pools
- dirty flag sync
- WebGPU instancing descriptors

## Run

```bash
npm run dev
```

Open `examples/performance-hot-paths-demo/index.html` through the Vite dev server.
