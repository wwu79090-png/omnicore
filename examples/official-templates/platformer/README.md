# Official OmniCore Platformer Template

Run:

```bash
npm install
npm run dev
```

This starter keeps everything in `src/main.js`: gravity, platforms, a collectible loop, and a tiny enemy patrol. It is intended as the shortest playable side-scroller baseline for new OmniCore users.
