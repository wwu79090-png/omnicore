# Official OmniCore RPG Dialogue Template

Run:

```bash
npm install
npm run dev
```

This template demonstrates a compact RPG scene with keyboard movement, an NPC dialogue state, inventory pickup, and an HTML/CSS overlay that stays outside the renderer.
