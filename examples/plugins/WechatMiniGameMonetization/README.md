# WechatMiniGameMonetization

Official OmniCore plugin example for WeChat Mini Game payment and advertisement providers.

Run the demo page to see mocked `wx.requestMidasPayment`, rewarded video, and banner provider calls through `OmniCore.Payment` and `OmniCore.Ad`.
